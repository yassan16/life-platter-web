# 開発環境デフォルト（npm run dev 時に自動適用）
# クライアントサイドは相対パスを使用し、Next.js rewrites でプロキシ
# これによりCORSエラーを回避（ブラウザ → Next.js:3000 → Nginx:80 → FastAPI）
NEXT_PUBLIC_API_URL=

# rewrites先（サーバーサイド専用、NEXT_PUBLIC不要）
API_REWRITE_DESTINATION=http://localhost

NEXT_PUBLIC_IMAGE_HOST=localhost
