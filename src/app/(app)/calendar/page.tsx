import { MonthCalendar } from '@/components/features/calendar/MonthCalendar';

export const metadata = {
  title: 'カレンダー - Life Platter',
};

export default function CalendarPage() {
  return <MonthCalendar />;
}
